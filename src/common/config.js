const baseUrl = "https://swapi.co/api";

const config = {
  "loginUrl": baseUrl + "/people/?search=",
  "planetsUrl": baseUrl + "/planets/?search=",
  "allowedUser": "Luke skywalker"
};

export default config;